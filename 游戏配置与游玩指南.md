# 视觉小说 · 制作与游玩指南

这是一个基于 Unity 的 Windows 视觉小说工程。当前版本支持主菜单、AVG 剧情、打字机文本、历史记录、玩家输入名字、变量与条件跳转、隐藏 true story、桌面宠物、true 线桌宠/AVG 形态互切、配音、资源打包加密、锁死结局和彩蛋剧情。

- Unity 版本：**2023.1.22f1**
- 目标平台：**Windows 桌面**
- 主要配置入口：`Assets/StreamingAssets/`
- 游戏运行入口由代码自动创建，不需要手动往场景里拖脚本或预制体。

---

## 一、快速开始

1. 用 **Unity 2023.1.22f1** 打开工程。
2. 打开 `Assets/Scenes/SampleScene.unity`。实际上任意场景都可以，游戏会由启动代码自动初始化。
3. 点击 Unity 顶部 **Play**。
4. 主菜单点击「开始游戏」，进入 `Assets/StreamingAssets/story.json` 配置的普通剧情。

常用编辑器菜单：

| 菜单 | 用途 |
|---|---|
| `Tools/VN Story Editor` | 表单式编辑剧情节点 |
| `Tools/VN Node Graph` | 节点图方式查看/编辑剧情流 |
| `打包/打包 Windows(64位)` | 使用项目内置脚本打包 Windows 版本 |

---

## 二、游玩操作

### AVG 剧情

| 操作 | 说明 |
|---|---|
| 鼠标左键点击画面 | 推进对话；文字未显示完时会先跳过打字机动画 |
| 点击选项 | 进入对应分支 |
| 输入名字后点按钮 | 保存输入内容到变量，例如 `%playername%` |
| 点击历史记录按钮 | 打开对话历史，背景压暗，可返回当前对话 |
| 连按三次 `M` | 真重置：清除结局标记并回到 AVG 开头；名字输入框激活时不会触发 |

### 桌面宠物

| 操作 | 说明 |
|---|---|
| 鼠标移到宠物不透明区域 | 窗口变为可交互 |
| 鼠标不在宠物上 | 点击穿透到桌面，不挡正常操作 |
| 左键点宠物 | 触发点击表情与随机台词 |
| 左键拖动宠物 | 移动宠物，拖动时切换拖拽表情 |
| 在头顶区域来回移动鼠标 | 触发摸头反应 |
| 右键宠物 | 打开菜单；普通桌宠可退出，true 线解锁后还可「切换成对话」 |

桌宠会自动：

- 每 5-10 分钟随机说一句待机台词。
- 玩家 2 分钟没有鼠标/键盘操作时睡着。
- 玩家离开一段时间再回来时说欢迎台词。
- 按时段触发早/中/晚/深夜问候。
- 偶尔眨眼、挥手、wink 或自己溜达。

### true 线结局 AVG 形态

true 线到达特殊结局后，桌宠右键菜单会出现「切换成对话」。切换后会显示一个悬浮 AVG 对话框：

- 对话配置来自 `Assets/StreamingAssets/ending_avg.json`。
- 每隔一段时间自动弹一句台词。
- 玩家 2 分钟不操作会睡着。
- 点击「变回桌宠」按钮可回到桌宠形态。

---

## 三、剧情文件

主要剧情文件：

| 文件 | 用途 |
|---|---|
| `Assets/StreamingAssets/story.json` | 普通线主剧情 |
| `Assets/StreamingAssets/story_true.json` | 隐藏 true story |
| `Assets/StreamingAssets/ending_avg.json` | true 线结局后悬浮 AVG 对话框 |
| `Assets/StreamingAssets/pet_dialogues.json` | 桌宠台词、睡眠、点击、摸头、随机动画等 |
| `Assets/StreamingAssets/神秘指导.txt` | 打包后保留明文，给玩家翻文件时看到的提示 |

JSON 支持 `//` 与 `/* ... */` 注释，运行时会自动去掉注释再解析。仍然要注意：不要多写尾逗号，字符串里的双引号要转义。

---

## 四、`story.json` 配置说明

### 4.1 整体结构

```json
{
  "startNode": "n1",
  "tamperNode": "tamper",
  "historyBlockedMessage": "",
  "nodes": []
}
```

| 字段 | 说明 |
|---|---|
| `startNode` | 剧情起点节点 ID |
| `tamperNode` | 锁死结局后，玩家删除可见存档时播放的彩蛋入口 |
| `historyBlockedMessage` | 非空时历史按钮仍可点，但不会显示真实历史，只显示这句话。true 线使用了这个功能 |
| `nodes` | 所有剧情节点 |

### 4.2 节点字段

| 字段 | 说明 |
|---|---|
| `id` | 节点唯一 ID，被 `next`、选项、条件跳转引用 |
| `background` | 背景图名，不带扩展名；空字符串表示保持上一张背景 |
| `characters` | 当前立绘列表；空数组 `[]` 表示清空立绘 |
| `keepCharacters` | `true` 表示不改动当前立绘 |
| `speaker` | 说话人名字；空字符串表示旁白 |
| `text` | 正文，支持 `\n` 换行、`%变量名%` 替换 |
| `voice` | 配音文件名，不带路径；会从 `voice/` 里加载 |
| `next` | 无选项时点击后跳转到的节点 |
| `choices` | 选项列表；有选项时通常不使用 `next` |
| `ending` | 结局类型，见第八节 |
| `inputVariable` | 非空时显示输入框，并把玩家输入保存到这个变量 |
| `inputPlaceholder` | 输入框提示语 |
| `inputButtonText` | 输入按钮文字 |
| `secretName` | 隐藏真名；玩家输入内容与它一致时触发隐藏线 |
| `secretStory` | 隐藏线剧本文件名，例如 `story_true.json` |
| `secretNext` | 切到隐藏线后进入哪个节点；空则用隐藏剧本的 `startNode` |
| `setVariables` | 进入该节点时修改变量 |
| `conditionalNexts` | 进入该节点时按变量条件跳转 |

### 4.3 立绘配置

```json
{
  "characters": [
    { "sprite": "xiaoman_gentle", "position": "center" }
  ]
}
```

- `sprite` 对应 `Assets/StreamingAssets/images/xiaoman_gentle.png`。
- `position` 支持 `left`、`center`、`right`。
- `keepCharacters: true` 适合旁白或结局描述，保持当前立绘不变。

### 4.4 选项配置

```json
{
  "choices": [
    {
      "text": "我留下来陪你。",
      "next": "pre_pet"
    },
    {
      "text": "我还是得走。",
      "next": "pre_lock"
    }
  ]
}
```

选项也可以设置变量：

```json
{
  "text": "我是创造你的人",
  "next": "next_node",
  "setVariables": [
    { "name": "player_identity", "operation": "set", "value": "creator" }
  ]
}
```

### 4.5 变量与条件跳转

变量修改支持三种：

```json
{ "name": "trust", "operation": "set", "value": "1" }
{ "name": "trust", "operation": "add", "amount": 1 }
{ "name": "trust", "operation": "subtract", "amount": 1 }
```

条件跳转示例：

```json
{
  "id": "gate",
  "conditionalNexts": [
    {
      "variable": "trust",
      "comparison": "greaterOrEqual",
      "equals": "3",
      "next": "good_route"
    }
  ],
  "next": "normal_route"
}
```

`comparison` 支持：

- `equals`
- `notEquals`
- `greater`
- `greaterOrEqual`
- `less`
- `lessOrEqual`

### 4.6 玩家名字与隐藏 true story

普通线的名字输入节点可以这样写：

```json
{
  "id": "name_input",
  "speaker": "小满",
  "text": "告诉我你的名字吧。",
  "inputVariable": "playername",
  "inputPlaceholder": "请输入你的名字",
  "inputButtonText": "告诉小满",
  "secretName": "小满",
  "secretStory": "story_true.json",
  "secretNext": "s_start",
  "next": "normal_next"
}
```

- 玩家输入普通名字：继续走 `next`。
- 玩家输入 `secretName`：立即切到 `secretStory` 的 `secretNext` 节点继续演出。
- 已有变量会带到新剧本里，所以 true 线仍可使用 `%playername%`。
- `secretName` 当前按需求保存在 JSON 明文里；打包时 JSON 会进入加密 `game.pak`。

---

## 五、素材配置

### 5.1 图片

图片放在 `Assets/StreamingAssets/images/`。

| 用途 | 示例 | 建议 |
|---|---|---|
| 背景 | `bg_room.png` | 1920×1080 |
| 普通线立绘 | `xiaoman_gentle.png` | 透明 PNG，半身 |
| true 线立绘 | `true_happy.png` | 透明 PNG，比例尽量与普通线一致 |
| 桌宠 | `pet.png`、`pet_sleep.png`、`pet_click.png` | 透明 PNG，小尺寸 chibi 风格 |

JSON 里写图片名时不带扩展名，例如写 `"sprite": "true_happy"`，文件是 `true_happy.png`。

### 5.2 配音

配音文件放在 `Assets/StreamingAssets/voice/`。

在剧情节点或桌宠台词中写：

```json
{
  "text": "你好呀。",
  "voice": "n1.wav"
}
```

支持 `.wav`、`.ogg`、`.mp3`。打包后音频会进入 `game.pak`，不会作为明文文件暴露。

---

## 六、桌宠配置 `pet_dialogues.json`

这个文件控制桌宠行为与台词。

常用字段：

| 字段 | 说明 |
|---|---|
| `idleMinSeconds` / `idleMaxSeconds` | 随机待机台词间隔 |
| `sleepAfterSeconds` | 玩家无操作多久后睡觉 |
| `awayThresholdSeconds` | 离开多久后回来触发欢迎 |
| `normalSprite` | 普通待机图 |
| `sleepSprite` | 睡觉图 |
| `clickSprite` | 点击反应图 |
| `dragSprite` | 拖拽图 |
| `headpatSprite` | 摸头图 |
| `walkSprite` | 溜达图 |
| `idle` | 待机随机台词库 |
| `click` | 点击台词库 |
| `morning` / `noon` / `evening` / `night` | 时段问候 |
| `welcome` | 欢迎回来台词 |
| `headpat` | 摸头台词 |
| `animations` | 随机小动画帧 |

台词格式：

```json
{
  "text": "你继续忙，我会乖乖在旁边待着。",
  "voice": "",
  "sprite": "pet_blink",
  "duration": 6
}
```

动画格式：

```json
{
  "frames": ["pet", "pet_wink", "pet"],
  "frameSeconds": 0.18,
  "loops": 1
}
```

---

## 七、true 线 AVG 配置 `ending_avg.json`

true 线结局后可在桌宠和 AVG 对话框之间互切。AVG 对话框使用独立 JSON：

```json
{
  "speaker": "小满",
  "portraitSprite": "true_gentle",
  "sleepSprite": "true_quiet",
  "sleepText": "……（歪着头睡着了）Zzz",
  "switchButtonText": "变回桌宠",
  "talkMinSeconds": 22,
  "talkMaxSeconds": 48,
  "sleepAfterSeconds": 120,
  "enter": [
    { "text": "换个方式陪你。", "sprite": "true_gentle" }
  ],
  "lines": [
    { "text": "我一直都在这儿。", "sprite": "true_gentle" }
  ]
}
```

- `enter`：刚切到 AVG 形态时说的台词。
- `lines`：之后随机弹出的台词。
- `sleepSprite` 目前可用占位图，之后有新睡觉立绘可直接替换。

---

## 八、结局与状态

### 8.1 普通桌宠结局：`"ending": "pet"`

到达后：

1. 写入桌宠标记。
2. 游戏窗口变成透明、置顶、点击穿透的桌宠窗口。
3. 下次启动直接进入桌宠模式。

透明效果只在 Windows 打包版生效。Unity 编辑器内会显示深色背景，便于调试。

### 8.2 true 线桌宠 + AVG 互切结局：`"ending": "petavg"`

到达后：

1. 写入桌宠标记。
2. 写入 `avgUnlocked` 标记。
3. 进入桌宠模式。
4. 右键桌宠可选择「切换成对话」，进入 `ending_avg.json` 配置的悬浮 AVG 形态。

### 8.3 锁死结局：`"ending": "lock"`

锁死结局使用双层标记：

| 标记 | 位置 | 作用 |
|---|---|---|
| 可见存档 | exe 同级目录的 `存档.sav` | 玩家能找到、能删的文件 |
| 隐藏标记 | 注册表 `HKCU\Software\CGJPetGame` + `%LOCALAPPDATA%\.cgjpet_marker` | 真正判断结局状态 |

如果玩家删掉可见存档但隐藏标记还在，游戏会认为被篡改，播放 `tamperNode` 指向的彩蛋剧情，然后仍然锁死。

---

## 九、历史记录

AVG 播放时可以打开历史记录：

- 显示当前周目已播放的对话。
- 打开时屏幕压暗。
- 点击返回回到当前对话。

true 线可以通过 `historyBlockedMessage` 禁止查看真实历史：

```json
{
  "historyBlockedMessage": "你在现实世界聊天的时候也有历史可翻吗？"
}
```

设置后，历史按钮仍可点击，但只显示这句话。

---

## 十、打包与发布

推荐使用项目内置菜单：

1. 点击 Unity 顶部 `打包/打包 Windows(64位)`。
2. 输出目录为 `Build/cgjpetgame/`。
3. 成功后会生成 `game.exe`。

当前打包后目录大致是：

```text
Build/cgjpetgame/
├── game.exe
├── UnityPlayer.dll
└── game_data/
    └── StreamingAssets/
        ├── game.pak
        └── 神秘指导.txt
```

说明：

- `game.exe` 必须和 `UnityPlayer.dll` 同级，这是 Unity Windows 程序的要求；`UnityPlayer.dll` 不能挪进文件夹。
- `game_data` 是 Unity 真正运行需要的数据目录。因为 exe 叫 `game.exe`，这个目录也必须叫 `game_data` 才能正常启动。
- `story.json`、`story_true.json`、图片、语音、桌宠配置等会被打包进 `game_data/StreamingAssets/game.pak`，不会明文暴露。
- `神秘指导.txt` 会保留明文，位于 `game_data/StreamingAssets/`。
- 发布给玩家时，把 `game.exe`、`UnityPlayer.dll`、`game_data/` 一起发出去。

如果桌宠打包后黑屏，检查：

- `Project Settings → Player → Resolution and Presentation`
- 关闭 `Use DXGI Flip Model Swapchain for D3D11`

当前工程已设置为关闭。

---

## 十一、开发与调试

| 操作 | 说明 |
|---|---|
| 编辑器内按 `F9` | 直接进入桌宠预览，不写入存档 |
| 编辑器内按 `F12` | 清空所有结局标记并回主菜单 |
| 编辑器内按 `F11` | 清空所有结局标记并直接进入 AVG 开头 |
| 打包版连按三次 `M` | 清空结局标记并回到 AVG 开头 |

手动完全重置：

1. 删除 exe 同级的 `存档.sav`。
2. 删除注册表 `HKCU\Software\CGJPetGame`。
3. 删除 `%LOCALAPPDATA%\.cgjpet_marker`。

常见问题：

- JSON 改坏：Unity Console 会报解析错误。
- 节点 ID 写错：运行时会找不到节点并返回主菜单/报错。
- 图片不显示：确认图片在 `Assets/StreamingAssets/images/`，且 JSON 里不写扩展名。
- 配音不播放：确认文件在 `Assets/StreamingAssets/voice/`，文件名与 `voice` 字段一致。
- 桌宠点击不到：桌宠使用像素级透明区域判定，只有不透明部分可交互。

---

## 十二、文件结构速查

```text
Assets/
├── Scripts/
│   ├── Core/      启动、GameManager、存档、pak 加密加载
│   ├── Story/     剧情模型、JSON 加载、图片/音频加载、WAV 解码
│   ├── UI/        UI 主题与主菜单
│   ├── VN/        AVG 播放器、历史记录、输入框、选项
│   ├── Pet/       桌宠、Windows 透明窗口、true 线 AVG 对话框
│   └── Editor/    打包脚本、pak 后处理、节点编辑器
└── StreamingAssets/
    ├── story.json
    ├── story_true.json
    ├── pet_dialogues.json
    ├── ending_avg.json
    ├── 神秘指导.txt
    ├── images/
    └── voice/
```

大多数剧情与玩法调整只需要改 `StreamingAssets` 里的 JSON、图片和语音文件。
